import styled from 'styled-components';

const HeaderContainer = styled.header`
	margin-left: 25%;
`;

function Header() {
	return (
		<HeaderContainer>
			<h1>Vhalo</h1>
		</HeaderContainer>
	);
}

export default Header;
